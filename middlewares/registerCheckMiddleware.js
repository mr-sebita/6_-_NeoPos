/* REGISTER CHECK MIDDLEWARE
 *
 * @link   
 * @file   This files defines a middleware check the data user entry in 
 *          the register form.
 * @author Martin, Mauricio, Jesus, Seba
 * @since  2020
*/

/**
 * Modules
 */
const { check, validationResult, body } = require('express-validator')

/**
 * Middleware
 */
function registerCheckMiddleware( req , res , next ){

} 

module.exports= registerCheckMiddleware; 