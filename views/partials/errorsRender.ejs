  <!-- show errors -->
    <% if(typeof errors != 'undefined'){ %>
    <ul>
        <% for(var i = 0; i < errors.length; i++) { %>
        <div class="alert alert-danger" role="alert">
            <%= errors[i].msg %>
        </div>
        <% } %>
    </ul>
    <% } %>